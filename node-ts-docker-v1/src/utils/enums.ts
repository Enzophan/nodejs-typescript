export enum USERTYPE {
    ADMIN = "admin",
    USER = "user",
}

export enum MODULES {
    USER= "user",
    ROLE= "role",
    POST= "post",
    PRODUCT= "product",
}

export enum ACTIONS {
    VIEW = "view",
    ADD = "add",
    EDIT = "edit",
    DELETE = "delete",
}